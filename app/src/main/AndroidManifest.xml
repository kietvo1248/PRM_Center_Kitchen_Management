<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.prm_center_kitchen_management">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRM_Center_Kitchen_Management">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.auth.LoginActivity" />
        <activity android:name=".activity.roles.AdminMenuActivity" />
        <activity android:name=".activity.roles.ManagerMenuActivity" />
        <activity android:name=".activity.roles.SupplyCoordinatorActivity" />
        <activity android:name=".activity.roles.KitchenStaffActivity" />
        <activity android:name=".activity.roles.FranchiseStaffActivity" />
    </application>

</manifest>